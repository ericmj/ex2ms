language: erlang
otp_release:
  - 17.0
before_install:
  - wget https://github.com/elixir-lang/elixir/releases/download/v0.14.0/Precompiled.zip -O elixir.zip
  - unzip -d elixir elixir.zip
  - export PATH=$(pwd)/elixir/bin:$PATH
script:
  - mix test
notifications:
  recipients:
    - martin@schuerrer.org
